/*
import React from 'react';
import { useState, useEffect } from 'react'

const AppBodyContentHandlers = (props) => {
    const appBodyContent = props.state;
    let setAppBodyContent = props.func;
}

export const handleSetTimerId = (e) => {
    e.preventDefault();
    let key = '';
    let timerId = '';
    for (key in e.value) {
        if (e.value.hasOwnProperty(key)) {
            let value = e.value[key];
            console.log("In handleSetTimerId - value: " + value);
            timerId = timerId + value;
        }
    }
    console.log("In handleSetTimerId - timerId: " + timerId);
    let setAppBodyContent = props.func;
    let msg = "Update Complete.  In the future select with " + timerId + "to pair with this device"
    setAppBodyContent('SetTimerIdCompleted', props.func, msg);
}

export const handleSetTimerId2 = (e) => {
}

*/


